# Ignore everything except checksums.txt and .gitignore.
# Agent binaries are no longer embedded in the server binary.
# checksums.txt is a tracked dev placeholder (empty); the release
# build overwrites it with real checksums via generate-embedded-checksums.sh.
*
!checksums.txt
!/.gitignore
