Batch:    {{batch}}
Total:    {{total}}
Success:  {{successful}}
{{#if timeout}}Note: partial results (timeout reached){{/if}}

Results:
{{#each jobs}}  OK {{job-type}}/{{job-id}} ({{datastore}}){{#if error}} - {{error}}{{/if}}
{{/each}}
